import { createTheme } from "@mui/material/styles";

export const customTheme = createTheme({
  palette: {
    primary: {
      main: "#1b1b1b",
    },
    secondary: {
      main: "#85ea2d",
    },
    background: {
      default: "#fafafa",
      paper: "#fbfcfe",
    },
  },
});
