// ROUTE NAMES

const root = "/";
const app = "/app";

export const ROUTES = {
  ROOT: root,
  APP: app,
  DASHBOARD: `${app}/dashboard`,
  SPECIES: `${app}/species`,
  // EMPLEADOS: `${app}/empleados`,
  // MENSAJES: `${app}/mensajes`,
  // ORDENES: `${app}/ordenes`,
};
