// ROUTE NAMES

const root = "/";
const app = "/app";

export const ROUTES = {
  ROOT: root,
  APP: app,
  DASHBOARD: `${app}/dashboard`,
  // SOLICITUDES: `${app}/solicitudes`,
  // EMPLEADOS: `${app}/empleados`,
  // MENSAJES: `${app}/mensajes`,
  // ORDENES: `${app}/ordenes`,
};
